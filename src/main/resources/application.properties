spring.profiles.active=${SPRING_PROFILES_ACTIVE:test}

jwt.secret=${JWT_SECRET:helpdesk-dev-secret-trocar-em-producao}
jwt.expiration=${JWT_EXPIRATION:1800000}

cors.origins=${CORS_ORIGINS:http://localhost:4200}

